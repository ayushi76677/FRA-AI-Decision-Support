import {useState} from 'react'
export default function SatelliteComparison({evidence}) { const [split,setSplit]=useState(50);return <section className="satellite-comparison"><div className="panel-head"><div><p className="eyebrow">SATELLITE EVIDENCE · SYNTHETIC DEMO DATA</p><h2>Before / after comparison</h2></div><span className="tag">{evidence?.data_label||'Requires verification'}</span></div><div className="compare-scene"><div className="compare-before"><b>BEFORE</b><span>Earlier synthetic observation</span></div><div className="compare-after" style={{clipPath:`inset(0 0 0 ${split}%)`}}><b>AFTER</b><span>Possible change overlay</span><i/></div><div className="compare-divider" style={{left:`${split}%`}}><em>↔</em></div><input aria-label="Compare before and after satellite views" type="range" min="0" max="100" value={split} onChange={e=>setSplit(e.target.value)}/></div><div className="compare-meta"><span>Before: {evidence?.before_date||'Observation unavailable'}</span><span>After: {evidence?.after_date||'Observation unavailable'}</span><span>{evidence?.source||'Source unavailable'}</span></div><p className="legal-note">Requires verification. Satellite change does not establish illegality, ownership, intent, or a violation.</p></section> }
