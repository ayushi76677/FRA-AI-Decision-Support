export default function SatelliteComparison({evidence}){const view=(label,date,changed)=><div className={`sat-view ${changed?'after':'before'}`}><b>{label}</b><div className="sat-art" aria-label={`${label} synthetic demo satellite view`}>SYNTHETIC<br/>DEMO SATELLITE VIEW</div><small>{date||'Observation unavailable'}</small></div>;return <section className="satellite-comparison"><div className="panel-head"><div><p className="eyebrow">SATELLITE EVIDENCE</p><h2>Before / after comparison</h2></div><span className="tag">{evidence?.data_label||'Loading'}</span></div><div className="sat-views">{view('BEFORE',evidence?.before_date,false)}{view('AFTER',evidence?.after_date,Boolean(evidence?.change_detected))}</div></section>}
