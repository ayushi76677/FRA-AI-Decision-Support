export default function ChangeEvidenceCard({evidence}) { if (!evidence) return <div className="empty">Loading satellite change evidence...</div>; return <section className="panel change-card"><div className="panel-head"><div><h2>{evidence.status?.replaceAll('_',' ')}</h2><p className="muted">{evidence.source} · {evidence.source_type}</p></div><span className="tag">{evidence.change_detected?'Possible change':'No change'}</span></div><p>{evidence.observation||'No satellite observation is available.'}</p><div className="spatial-grid"><div><span>Change area</span><strong>{evidence.change_area_hectares??'—'} ha</strong></div><div><span>Claim intersection</span><strong>{evidence.claim_intersection?'Yes':'No'}</strong></div><div><span>Claim area affected</span><strong>{evidence.intersection_percentage??'—'}%</strong></div></div><h3>Evidence supporting review</h3>{(evidence.evidence_for_review||[]).map(x=><p className="spatial-flag" key={x}>{x}</p>)}<h3>Evidence requiring caution</h3>{(evidence.limitations||[]).map(x=><p className="limitation" key={x}>{x}</p>)}<p><strong>Next action:</strong> {evidence.recommended_action||'Human review required.'}</p></section> }
